<VirtualHost *:80>
   ProxyRequests off

   Header add Set-Cookie "ROUTEID=.%{BALANCER_WORKER_ROUTE}e; path=/" env=BALANCER_ROUTE_CHANGED
   <Proxy balancer://frontend>
      BalancerMember http://[IP CONTAINER FRONT]:[PORT] route=[HOST ID CONTAINER]
      BalancerMember http://[IP CONTAINER FRONT]:[PORT] route=[HOST ID CONTAINER]
      [...]
      ProxySet stickysession=ROUTEID
      ProxySet lbmethod = byrequests
   </Proxy>

   <Proxy balancer://backend>
      BalancerMember http://[IP CONTAINER BACK]:[PORT]
      BalancerMember http://[IP CONTAINER BACK]:[PORT]
      [...]
      ProxySet lbmethod = byresquests
   </Proxy>

   ProxyPass /api balancer://backend
   ProxyPass /    balancer://frontend
   ProxyPassReverse /api balancer://backend
   ProdyPassReverse /	 balancer://frontend

</VirtualHost>